# Voice agent (Pipecat). Pin minor versions in your own lockfile after first install.
pipecat-ai[deepgram,google,silero]>=0.0.50
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6
loguru>=0.7

# Tests
pytest>=8.0
pytest-asyncio>=0.23
